import { ScrollView, View, Text } from 'react-native'
import React from 'react'
import { Link } from 'expo-router'

const Sex = () => {
    return (
        <ScrollView>

            <View style={{ height: "100%", width: "100%", backgroundColor: "black", paddingTop: 20 }}>
                <Text style={{ color: "white" }}>Sex</Text>
                </View>
        </ScrollView>
    )
}

export default Sex